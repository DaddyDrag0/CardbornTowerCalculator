const AUTO_CORRUPTED_ABILITY='CursedRetribution';
function thumbIds(c){return[aid(c?.croppedImageId),aid(c?.imageId)].filter(Boolean)}
function img(c){const map=window.CARDBORN_THUMBNAILS||{};for(const id of thumbIds(c)){if(map[id])return map[id]}return''}
function portrait(c,cls='mini-portrait'){const u=img(c),l=esc((c?.name||'?')[0]);return`<span class="${cls}"><span class="fallback-letter">${l}</span>${u?`<img src="${u}" alt="" loading="lazy" onerror="this.style.display='none'">`:''}</span>`}
function resolveRobloxThumbs(){}
function infiniteStats(floor){const f=Math.max(1,Math.floor(Number(floor)||1)),atk=Math.max(1,Math.floor(2500000+(100000000-2500000)*((f-1)/99)));return{hp:atk*2,atk}}
function floorSeed(seed,floor){let x=(seed^Math.imul(floor>>>0,0x9e3779b1))>>>0;x^=x>>>16;x=Math.imul(x,0x85ebca6b)>>>0;x^=x>>>13;x=Math.imul(x,0xc2b2ae35)>>>0;return(x^(x>>>16))>>>0}
function runSeconds(x){return x.actions*(.20/state.speed)+x.changes*(.40/state.speed)}
function sideName(f){return f?.side==='p'?'Allies':'Enemies'}
function dbgSnap(f){return f?{name:f.card?.name||'Unknown',ability:f.ability?.name||'',team:sideName(f),hp:Math.max(0,Math.floor(f.hp||0)),maxHp:Math.max(1,Math.floor(f.max||1)),damage:Math.max(0,Math.floor(f.atk||0)),skip:Math.max(0,Math.floor(f.skip||0)),frozen:Math.max(0,Math.floor(f.frozen||0)),shieldHp:Math.max(0,Math.floor(f.shieldHp||0)),shields:Math.max(0,Math.floor(f.shields||0)),dead:!!f.dead}:null}
function teamDbg(a){return(a||[]).slice(0,8).map(dbgSnap)}
function captureBattle(pt,et){return[...pt,...et].map(f=>({ref:f,s:dbgSnap(f)}))}
function unitDiff(a,b,label){if(!a||!b)return[];const out=[],tag=label?label+' ':'';if(a.hp!==b.hp)out.push(`${tag}HP ${compact(a.hp)} → ${compact(b.hp)}`);if(a.maxHp!==b.maxHp)out.push(`${tag}Max HP ${compact(a.maxHp)} → ${compact(b.maxHp)}`);if(a.damage!==b.damage)out.push(`${tag}ATK ${compact(a.damage)} → ${compact(b.damage)}`);if(a.shieldHp!==b.shieldHp)out.push(`${tag}shield ${compact(a.shieldHp)} → ${compact(b.shieldHp)}`);if(a.shields!==b.shields)out.push(`${tag}blocks ${a.shields} → ${b.shields}`);if(a.skip!==b.skip)out.push(`${tag}stun ${a.skip} → ${b.skip}`);if(a.frozen!==b.frozen)out.push(`${tag}freeze ${a.frozen} → ${b.frozen}`);return out}
function battleDiff(before,pt,et){const now=[...pt,...et],out=[];for(const old of before){const f=old.ref;if(!now.includes(f))continue;const n=dbgSnap(f),changes=unitDiff(old.s,n,f.card?.name||'card');if(changes.length)out.push(...changes)}for(const f of now){if(!before.some(x=>x.ref===f)){const s=dbgSnap(f);out.push(`spawned ${s.name} (${compact(s.hp)} HP · ${compact(s.damage)} ATK)`)}}return out}
function pushAbility(events,turn,f,phase,before,pt,et,extra=[]){if(!f)return;const diff=battleDiff(before,pt,et),bits=[...extra,...diff];if(!bits.length)return;events.push({turn,type:'ability',team:sideName(f),card:f.card?.name||'Unknown',detail:`${f.ability?.name||'Ability'} · ${phase}${bits.length?' · '+bits.join(' · '):''}`})}
const START_ABILITIES=new Set(['Purifying Light','Swarm Strike','Tip the Guard','Inspiring Leadership',"King's Rule",'Arachnid Dominion','Greed','Solar Ward',"Earth's Blessing",'Cursed Forge','Crushing Cold']);
function startPhase(own,foes,cid,events){const before=captureBattle(own,foes);teamStart(own,foes,cid);const diff=battleDiff(before,own,foes);if(!diff.length)return;const sources=own.filter(f=>START_ABILITIES.has(f.ability?.name));if(sources.length){for(const f of sources)events.push({turn:0,type:'ability',team:sideName(f),card:f.card.name,detail:`${f.ability.name} · Battle start · ${diff.join(' · ')}`})}else if(cid&&own.some(f=>f.flags?.corrupted)){const f=own.find(x=>x.flags?.corrupted);events.push({turn:0,type:'ability',team:sideName(f),card:f.card.name,detail:`Corrupted effect · Battle start · ${diff.join(' · ')}`})}}
function entryPhase(f,t,own,foes,r,ctx,events,turn){const before=captureBattle(own,foes);enter(f,t,own,foes,r,ctx);pushAbility(events,turn,f,'On entry',before,own,foes)}
function replayBattle(pt,et,r,cid,floor){const events=[],ctx={team:f=>f.side==='p'?pt:et};const initialAllies=teamDbg(pt),initialEnemies=teamDbg(et);startPhase(pt,et,cid,events);startPhase(et,pt,'',events);let pi=0,ei=0,actions=0,changes=0,loops=0,pf=pt[0],ef=et[0];entryPhase(pf,ef,pt,et,r,ctx,events,0);entryPhase(ef,pf,et,pt,r,ctx,events,0);let player=true;while(loops++<700){pi=next(pt,pi);ei=next(et,ei);if(pi<0)return{win:false,actions,changes,debug:{floor,initialAllies,initialEnemies,events,finalAllies:teamDbg(pt),finalEnemies:teamDbg(et)}};if(ei<0)return{win:true,actions,changes,debug:{floor,initialAllies,initialEnemies,events,finalAllies:teamDbg(pt),finalEnemies:teamDbg(et)}};pf=pt[pi];ef=et[ei];const f=player?pf:ef,own=player?pt:et,foes=player?et:pt;let t=player?ef:pf,turn=loops;if(f.frozen){events.push({turn,type:'stall',team:sideName(f),card:f.card.name,detail:'Frozen — turn skipped'});f.frozen--;player=!player;continue}if(f.skip){events.push({turn,type:'stall',team:sideName(f),card:f.card.name,detail:'Stunned — turn skipped'});f.skip--;player=!player;continue}
const beforeTurn=captureBattle(own,foes),oldTarget=t,pre=turnStart(f,t,own,foes,r,ctx);t=pre.target||t;const extra=[];if(pre.attacks>1)extra.push(`${pre.attacks} attacks`);if(Math.abs((pre.mult||1)-1)>.00001)extra.push(`attack ×${Number(pre.mult).toFixed(2).replace(/\.00$/,'')}`);if(t&&oldTarget&&t!==oldTarget)extra.push(`retargeted ${t.card.name}`);pushAbility(events,turn,f,'Turn effect',beforeTurn,own,foes,extra);
for(let hit=0;hit<pre.attacks;hit++){if(!t||t.dead)t=alive(foes)[0];if(!t)break;let m=pre.mult;if(f.ability.name==='Celestial Smite'&&hit===1)m*=1+p(f,0,.25);events.push({turn,type:'turn',team:sideName(f),card:f.card.name,detail:`vs ${t.card.name} | attacker ${Math.max(0,Math.floor(f.hp))}/${Math.max(1,Math.floor(f.max))} HP ${Math.max(0,Math.floor(f.atk))} ATK | defender ${Math.max(0,Math.floor(t.hp))}/${Math.max(1,Math.floor(t.max))} HP ${Math.max(0,Math.floor(t.atk))} ATK`});
const raw=Math.max(0,Math.floor(f.atk*m)),defBefore=dbgSnap(t),atkBefore=dbgSnap(f),lenBefore=foes.length,dealt=damage(f,t,raw,r,ctx,f.ability.name==='Perfect Strike');actions++;const defAfterHit=dbgSnap(t),atkAfterHit=dbgSnap(f);if(dealt!==raw||defBefore.shieldHp!==defAfterHit.shieldHp||defBefore.shields!==defAfterHit.shields||atkBefore.hp!==atkAfterHit.hp){const parts=[`incoming ${compact(raw)} → ${compact(dealt)} damage`,...unitDiff(defBefore,defAfterHit,t.card.name),...unitDiff(atkBefore,atkAfterHit,f.card.name)];events.push({turn,type:'ability',team:sideName(t),card:t.card.name,detail:`${t.ability?.name||'Defense effect'} · ${parts.join(' · ')}`})}
let killed=false;if(t.hp<=0){const preDieHp=t.hp,killer=f;killed=die(t,killer,foes,own,r,ctx);if(!killed&&t.hp>0){events.push({turn,type:'revive',team:sideName(t),card:t.card.name,detail:`${t.ability?.name||'Revive'} · ${compact(Math.max(0,preDieHp))} → ${compact(t.hp)} HP`})}else if(killed){events.push({turn,type:'death',team:sideName(t),card:t.card.name,detail:`Defeated by ${f.card.name} · ${compact(dealt)} damage`})}if(foes.length>lenBefore){for(const spawned of foes.slice(lenBefore))events.push({turn,type:'spawn',team:sideName(spawned),card:spawned.card.name,detail:`Spawned after ${t.card.name}'s defeat · ${compact(spawned.hp)} HP · ${compact(spawned.atk)} ATK`})}}
const beforeAfter=captureBattle(own,foes);after(f,t,dealt,own,foes,r,ctx,killed);pushAbility(events,turn,f,'After attack',beforeAfter,own,foes,killed?['enemy defeated']:[]);
if(killed){if(player){ei=next(et,ei+1);changes++;if(ei>=0){t=et[ei];entryPhase(t,f,et,pt,r,ctx,events,turn)}}else{pi=next(pt,pi+1);changes++;if(pi>=0){t=pt[pi];entryPhase(t,f,pt,et,r,ctx,events,turn)}}}if((player&&ei<0)||(!player&&pi<0))break}player=!player}
events.push({turn:loops,type:'stall',team:'System',card:'Battle',detail:'Turn limit reached'});return{win:next(et,Math.max(0,ei))<0,actions,changes,debug:{floor,initialAllies,initialEnemies,events,finalAllies:teamDbg(pt),finalEnemies:teamDbg(et)}}}
function replayFloor(t,seed,floor){const r=rng32(floorSeed(seed,floor)),pt=makePlayer(t),et=makeEnemy(floor,r);return replayBattle(pt,et,r,AUTO_CORRUPTED_ABILITY,floor)}
function simulate(ti,seed){const t=state.teams[ti],start=state.start,cap=state.cap;let cleared=start-1,actions=0,changes=0,last=[],death=cap,debug=null;for(let floor=start;floor<=cap;floor++){const r=rng32(floorSeed(seed,floor)),pt=makePlayer(t),et=makeEnemy(floor,r);last=et.slice(0,4).map(x=>x.card.name);const b=battle(pt,et,r,AUTO_CORRUPTED_ABILITY);actions+=b.actions;changes+=b.changes;if(!b.win){death=floor;debug=replayFloor(t,seed,floor).debug;return{seed,cleared,actions,changes,last,death,enemy:infiniteStats(floor),seconds:runSeconds({actions,changes}),debug}}cleared=floor}debug=replayFloor(t,seed,cap).debug;return{seed,cleared,actions,changes,last,death:cap+1,enemy:infiniteStats(cap),seconds:runSeconds({actions,changes}),debug}}
function summarize(a){const sorted=[...a].sort((x,y)=>x.cleared-y.cleared),fs=sorted.map(x=>x.cleared),n=fs.length,avg=fs.reduce((x,y)=>x+y,0)/n,med=fs[Math.floor((n-1)*.5)],rel=fs[Math.floor((n-1)*.10)],hi=fs[Math.floor((n-1)*.90)],seconds=a.reduce((x,y)=>x+y.seconds,0)/n;return{avg,med,rel,hi,best:fs[n-1],low:fs[0],runCount:n,seconds,runs:a.map((x,i)=>({...x,run:i+1}))}}
function run(indices){if(state.running)return;state.running=true;state.pAbilities=true;state.eAbilities=true;render();setTimeout(()=>{const base=Date.now()>>>0;for(const ti of indices){const a=[];for(let i=0;i<state.runs;i++)a.push(simulate(ti,(base+ti*0x9e3779b9+i*2654435761)>>>0));state.teams[ti].result=summarize(a)}state.running=false;save();render()},20)}
function tooltip(c,f){const a=ability(c,f),s=stats(c,f),isFabled=!!f?.fabled&&FABLED[c.ability?.name]&&FABLED[c.ability?.name]!=='None';return`<div class="tip-name">${esc(c.name)}</div><div class="tip-ability">${esc(a.name)}</div><div class="tip-desc">${esc(a.desc)}</div>${isFabled?`<div class="tip-fabled"><b>FABLED ABILITY ACTIVE</b></div>`:''}<div class="tip-rarity">1 / ${compact(s.rarity)} · ${compact(s.hp)} HP · ${compact(s.atk)} ATK</div>`}
function result(t,i){const r=t.result;if(!r)return'';const enemyCounts={};for(const run of r.runs)for(const n of run.last||[])enemyCounts[n]=(enemyCounts[n]||0)+1;const common=Object.entries(enemyCounts).sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])).slice(0,6);const endingHtml=common.length?`<div class="ending-enemies"><span>Most common losing-floor enemies</span><div class="enemy-chips">${common.map(([n,c])=>`<i>${esc(n)} <b>×${c}</b></i>`).join('')}</div></div>`:'';return`<section class="result-card ${i===state.team?'active-result':''}"><div class="result-head"><div><span class="kicker">TEAM ${i+1}</span><h4>Infinite Dungeon</h4></div><span class="verified">${r.runCount} RUN${r.runCount===1?'':'S'}</span></div><div class="result-metrics"><div><span>Average</span><b>F${r.avg.toFixed(1)}</b><small>mean clear</small></div><div><span>Median</span><b>F${r.med}</b><small>middle run</small></div><div><span>Reliable</span><b>F${r.rel}</b><small>90% reach</small></div><div><span>High roll</span><b>F${r.hi}</b><small>90th percentile</small></div><div><span>Battle time</span><b>${time(r.seconds)}</b><small>average · ${state.speed}×</small></div></div><div class="result-meta"><span>Range <b>F${r.low}–F${r.best}</b></span><span>${r.runCount} runs</span></div><div class="floor-strip">${r.runs.map((x,j)=>`<span role="button" tabindex="0" data-debug-team="${i}" data-debug-run="${j}" title="Run ${j+1}${x.death<=state.cap?' · lost Floor '+x.death:' · floor cap reached'}${x.last?.length?' · '+x.last.join(' / '):''} · click for debug">${x.death<=state.cap?x.death:x.cleared}</span>`).join('')}</div>${endingHtml}</section>`}
function render(){const t=cur(),q=norm(state.query),list=CARDS.filter(c=>!q||norm(c.name).includes(q)||norm(c.ability?.name).includes(q)||norm(c.ability?.desc).includes(q)).sort((a,b)=>b.rarityValue-a.rarityValue),readyN=state.teams.filter(ready).length,bq=norm(state.banQuery),suggest=POOL.filter(c=>!state.bans.includes(c.name)&&bq&&norm(c.name).includes(bq)).slice(0,8),results=state.teams.map(result).join('');
root.innerHTML=`<main class="shell"><header class="topbar"><div><p class="kicker">CARDBORN RNG</p><h1>Infinite Dungeon Calculator</h1></div><div class="theme-switch"><button data-theme="slate" class="${state.theme==='slate'?'on':''}">Slate</button><button data-theme="castle" class="${state.theme==='castle'?'on':''}">Castle</button></div></header><div class="team-tabs">${state.teams.map((x,i)=>`<button data-team="${i}" class="${i===state.team?'on':''}"><span>Team ${i+1}</span><i>${x.result?`Range F${x.result.low}–F${x.result.best}`:ready(x)?'Ready':'Empty'}</i></button>`).join('')}<button data-duplicate class="tab-action">Duplicate</button></div><section class="workspace"><article class="panel"><div class="panel-head"><div><span class="kicker">LOADOUT</span><h3>Team ${state.team+1}</h3></div><div class="panel-actions"><button class="text-button" data-copy>Copy code</button><button class="text-button" data-import>Import code</button><button class="text-button danger-lite" data-clear>Clear team</button></div></div><div class="team-list">${t.cards.map((s,i)=>{const c=BY.get(s.name);if(!c)return`<button class="team-row empty-team-card ${i===state.slot?'active':''}" data-slot="${i}" draggable="true"><span class="slot">0${i+1}</span>${portrait(null,'portrait')}<span class="card-copy"><span class="name-line"><b>Select a card</b></span><span class="numbers">Choose from the library</span></span></button>`;const st=stats(c,s.flags);return`<button class="team-row ${i===state.slot?'active':''}" data-slot="${i}" draggable="true" data-tip="${esc(c.name)}"><span class="slot">0${i+1}</span>${portrait(c,'portrait')}<span class="card-copy"><span class="name-line"><b>${esc(c.name)}</b><em>${esc(ability(c,s.flags).name)}</em></span><span class="numbers"><i><b>${compact(st.hp)}</b> HP</i><i><b>${compact(st.atk)}</b> ATK</i></span><span class="border-pills">${BORDERS.map(k=>`<label data-border="${k}" data-bi="${i}" class="${s.flags[k]?'on':''}">${k[0].toUpperCase()+k.slice(1)}</label>`).join('')}</span></span></button>`}).join('')}</div></article><article class="panel library"><div class="panel-head"><div><span class="kicker">CARDS</span><h3>Slot ${state.slot+1}</h3></div><small>${list.length} shown</small></div><input id="search" class="search" value="${esc(state.query)}" placeholder="Search card or ability…"><div class="library-list">${list.map(c=>`<button data-card="${esc(c.name)}" data-tip="${esc(c.name)}" class="${t.cards[state.slot].name===c.name?'selected':''}">${portrait(c)}<span><b>${esc(c.name)}</b><small>${esc(c.ability?.name||'No ability')}</small></span><em>1 / ${compact(c.rarityValue)}</em></button>`).join('')}</div></article><article class="panel simulation"><div class="panel-head"><div><span class="kicker">INFINITE TEST</span><h3>Run simulator</h3></div><small>${readyN}/5 teams ready</small></div><div class="simulation-body"><div class="sim-field"><span>Runs per team</span><div class="run-options">${RUNS.map(n=>`<button data-runs="${n}" class="${state.runs===n?'on':''}">${n}</button>`).join('')}</div></div><div class="sim-grid"><label class="sim-field"><span>Start floor</span><input id="start" type="number" min="1" max="${MAX_FLOOR}" value="${state.start}"></label><label class="sim-field"><span>Floor cap</span><input id="cap" type="number" min="1" max="${MAX_FLOOR}" value="${state.cap}"></label></div><label class="sim-field"><span>Battle speed</span><select id="speed">${SPEEDS.map(n=>`<option value="${n}" ${n===state.speed?'selected':''}>${n}×</option>`).join('')}</select></label><div class="depth-ban-box"><div class="depth-ban-head"><span>Infinite Dungeon bans</span><div>${state.bans.length?'<button class="depth-ban-clear" data-bclear>Clear</button>':''} <b>${state.bans.length}/3</b></div></div><small>Remove up to three non-secret cards from the enemy pool.</small>${state.bans.length?`<div class="depth-ban-chips">${state.bans.map((n,i)=>`<button class="depth-ban-chip" data-br="${i}">${esc(n)} ×</button>`).join('')}</div>`:''}<div class="depth-ban-search"><input id="bsearch" value="${esc(state.banQuery)}" placeholder="${state.bans.length>=3?'3/3 bans selected':'Search card to ban…'}" ${state.bans.length>=3?'disabled':''}>${bq&&state.bans.length<3?`<div class="depth-ban-suggestions">${suggest.length?suggest.map(c=>`<button data-ba="${esc(c.name)}"><span>${esc(c.name)}</span><small>${esc(c.ability?.name||'No ability')}</small></button>`).join(''):'<small>No matching cards.</small>'}</div>`:''}</div></div><div class="sim-actions">${state.running?'<button disabled class="secondary-run">Running…</button><button disabled class="sim-run">Simulating</button>':`<button data-runone class="secondary-run" ${ready(t)?'':'disabled'}>Test Team ${state.team+1}</button><button data-runall class="sim-run" ${readyN?'':'disabled'}>Test ${readyN} Ready Team${readyN===1?'':'s'}</button>`}</div></div></article></section><section class="results-panel"><div class="results-title"><div><span class="kicker">INFINITE DUNGEON RESULTS</span><h3>${results?'Team comparison':'No simulations yet'}</h3></div>${results?'<button class="text-button" data-rclr>Clear results</button>':''}</div>${results?`<div class="result-grid">${results}</div>`:'<div class="results-empty">Build a complete four-card team, set borders and bans, then run a test.</div>'}</section><footer><span>Cardborn Infinite Dungeon Calculator</span><span>${CARDS.length} cards · Slate / Castle</span></footer></main>`;bind();}
